# Gemini Burmese Launch Review

Route: local 9Router at `127.0.0.1:20128`
Requested model: `ag/gemini-3.7-flash-high`
Resolved model: `gemini-3.7-flash-tiered`

The first Gemini draft correctly announced launch but sounded formal. A second freer pass became natural but hallucinated prices, handles, automatic delivery, and performance claims, so it was rejected completely.

A third constrained pass received the exact factual candidate and was prohibited from changing facts. It returned the final image copy and caption unchanged. This version is used in the export.

For the R2 language re-audit, Gemini again confirmed that the prior copy was translation-shaped and overly formal. Its proposed rewrite introduced unnatural or incorrect terms including `ကွန့်မန့်` and `မဲခွဲစနစ်`, restored formal `ဖြစ်ပါသည်` constructions, and weakened the launch into a price notice. Those proposals were rejected. Only the diagnosis informed the final human-curated Burmese rewrite; no Gemini wording was accepted without independent review.

A final audit-only pass was given the locked R2 phrases, told to accept normal Myanmar code-switching, and allowed to flag only genuinely unnatural wording. It returned an empty issue list. Facts and links were then revalidated from the exported caption.

R4 received two additional Gemini passes. The first supplied audience structure but used formal constructions such as `တွဲဖက်အသုံးပြုနိုင်သည့်` and `ဖြစ်ပါသည်`; those were rejected. The second was audit-only and returned `PASS` for the locked image copy and caption. It incorrectly questioned the existence of Outline Foundation; Outline's official site confirms that Outline is owned by the Outline Foundation, so the accurate non-affiliation disclaimer was retained.
