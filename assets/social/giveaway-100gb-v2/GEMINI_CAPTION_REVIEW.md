# Gemini Burmese Caption Review

Route: local 9Router at `127.0.0.1:20128`
Requested model: `ag/gemini-3.7-flash-high`
Resolved model: `gemini-3.7-flash-tiered`

Two caption passes were used. The first removed formal legalistic phrasing and produced a more conversational entry explanation. The second returned the final caption as structured paragraphs while preserving every locked fact and URL.

Human verification changed only `data ပမာဏ` to the unambiguous `VPN Key အသုံးပြုခွင့်ပမာဏ`, retained the complete Facebook release, and removed generic `#FreeVPN` because the prize is a limited 100 GB key rather than an unlimited free-VPN product.

The actual exported final caption was submitted to Gemini once more with the locked facts. Final result: `APPROVED`, with no corrections.
